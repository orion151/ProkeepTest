import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  label: {
    textAlign: 'center',
    color: 'white',
    padding: 10,
    paddingLeft: 20,
    paddingRight: 20,
    margin: 20,
    borderRadius: 5,
    backgroundColor: '#10db16'
  },
  disbleButton: {
    backgroundColor: 'grey'
  }
})

export default styles
